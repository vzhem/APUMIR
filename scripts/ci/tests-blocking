# K6: тесты ядра блокируют проверку.
#
# Сам файл ничего не делает - его наличие читает scripts/ci/ci-core-check.sh.
# Как только этот маркер лежит в репозитории, падение `cargo test --lib`
# роняет проверку на pull request, то есть в main не попадает код с падающими
# тестами. Убрать файл = снова сделать тесты не блокирующими (без правок
# воркфлоу и без участия владельца).
#
# Включено 2026-09-17 после первого зелёного прогона проверки (K3-фикс +
# K4-1/K4-2/K4-3 + K5-1): ядро компилируется, мост генерируется, тесты зелёные.
