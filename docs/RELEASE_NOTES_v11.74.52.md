# APU v11.74.52 — служебные строки не видны в списках

## Что изменилось

Владелец (скрин «Проводника»): «гифка прописана буквами и цифрами» -
в списках светились служебные строки: «APUGIFREF1|b11…» (ссылка на
гифку, р.128) в предпросмотре группы, «APUSTK1|ask» (конверт
стикер-роя, р.139) в личном чате, «🖼 39513698-…» (файловая визитка
гифки-блоба с uuid-именем).

Новый util/ChatPreviews.kt: human(raw) - служебная строка ->
подпись («🖼 Гифка», «Стикеры»), обычный текст проходит насквозь.
Применён на ОТОБРАЖЕНИИ (чинит и существующие строки в базе):
- ContactCard (личные чаты «Проводника»);
- ChatListScreen (группы/каналы);
- GroupChatScreen (список тем);
и на ЗАПИСИ (новые сообщения сразу аккуратные):
- GroupRepository.preview() (registerTopicMessage/updateGroupLastMessage);
- ChatRepository: 3 вызова updateLastMessage (send/saveIncoming/
  insertLocalFileMessage); gif-ref пути уже писали «🖼 Гифка».

## Технические детали

- Внутри чатов НИЧЕГО не менялось - карточки гифок/стикеров как были
  (владелец: «внутри отображается нормально»).
- check86 FAILURE: Text в списке тем получил String? (у human()
  nullable-возврат, контракт isNullOrBlank() раньше давал non-null) -
  fix elvis ?: "" -> check87 SUCCESS. УРОК: обёртка в nullable-хелпер
  ломает смарт-касты от контрактов - сразу добавлять elvis в местах
  с contract-based nullability.
- v11.74.52 = 2983350, run 36008923856 SUCCESS, latest, APK
  40 500 694 Б, sha256 9a537473…7d8.
