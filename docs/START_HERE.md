# APU — вход в проект для НОВОГО ИИ-чата (свежесть 2026-09-09)

Этот файл — единственная точка входа. Дальше — картотека: один документ на одну
тему, дубликаты убраны.

## 0. Картотека документов

| Документ | Про что | Статус |
|---|---|---|
| `START_HERE.md` | вход, текущее состояние, правила репозитория, релиз | **этот файл** |
| `AI_HANDOFF.md` | правила работы от владельца, тестовые телефоны, незакрытые задачи, грабли | действующий |
| `BACKUP_AND_NEW_PC.md` | резервная копия, флешка, восстановление на новом ПК | действующий |
| `NEW_DEVELOPMENT_PC.md` | выбор железа, установка программ и SDK | действующий |
| `AI_COLLABORATION_NOTES.md` | журнал всех раундов; читать **с конца** | действующий |
| `MASTER_PLAN_v2.md` | архитектура и план | действующий |
| `SECURE_FILE_TRANSFER.md` | архитектура файлов (F4-срезы), верхний CURRENT OVERRIDE | действующий |
| `VERSION_STATISTICS.md` | статистика версий | **обрывается на v11.17.1** |
| `RELEASE_PUBLICATION_POLICY.md` | каким должно быть публичное описание релиза | действующий |
| `CALLS_BOOTSTRAP.md` | вход для чата про звонки: суть проекта, конвейер, карта кода, план | действующий |
| `IDENTITY_VAULT_SETUP.md` | хранилище личности на Cloudflare Worker: что нажимать владельцу | действующий |
| `MESH_DELIVERY.md` | доставка + **раздел про сквозное шифрование переписки** | действующий |
| `CHANNEL_SWARM_DESIGN.md` | раздача постов канала роем «как торрент» для 10 000+ подписчиков: пакеты, подпись манифеста, этапы | **проект, ждёт решений владельца** |
| `WALLPAPER_ICONS.md` | каталог 109 значков фирменных обоев | действующий |
| `PC_TRANSFER.md`, `RESTORE_ON_NEW_PC.md`, `FLASH_BACKUP_RUNBOOK.md`, `BACKUP_AND_CLEAN_PC_RECOVERY.md`, `NEXT_AI_CHAT_BOOTSTRAP.md` | — | заглушки, заменены |

Правило картотеки: факт живёт в одном документе. Нашёл противоречие между двумя
документами — правь тот, у которого тема совпадает, и не копируй текст в соседний.

## 1. Суть проекта (одно дыхание)

**APU** (`com.vladimir.messenger`, репо `vzhem/APUMIR`) — Android-мессенджер
с Rust-ядром. Главная цель владельца: **надёжная и максимально быстрая
передача файлов любого практического размера через любые сети БЕЗ своих
серверов хранения**. Серверов хранения нет: каждый телефон — сервер. Внешний
ресурс допустим только как временный провод для уже зашифрованных байтов.
Второй большой пласт — раздел ГРУППЫ/КАНАЛЫ «как в Telegram» и фирменный стиль
(темы день/ночь, обои, золотые акценты, всё по-русски, термин «@никнейм»).

## 2. Что прочитать до первой правки кода (по порядку)

1. этот файл целиком;
2. `docs/AI_HANDOFF.md` целиком — правила работы от владельца, тестовые
   телефоны, незакрытые задачи и грабли; этого нет больше нигде;
3. `docs/AI_COLLABORATION_NOTES.md` — **с конца** (последние ~200 строк:
   раунды 55–59, релизы v11.24.0–v11.26.0, резервная копия на флешке);
4. `docs/MASTER_PLAN_v2.md` и верхний `CURRENT OVERRIDE` в
   `docs/SECURE_FILE_TRANSFER.md` — архитектура файлов (F4-срезы);
5. только потом конкретный код.

## 3. Где мы сейчас (проверено 2026-09-11 ночью, не по памяти)

- Релиз **v11.70.11** = Latest (`gh api repos/vzhem/APUMIR/releases/latest --jq .tag_name`),
  `app-release.apk` = 37 169 462 байт, sha256 `0a08f13c…31f0`, тег на
  коммите `cd0fbc3`, прогон 34583792070 с первого раза; `main` = ветка.
  Описание - `docs/RELEASE_NOTES_v11.70.11.md`: рой, этап 5 - сообщения
  обычных групп > 30 участников идут через подписанный манифест (первая
  волна + эстафета сидов, куски по `mwant`), пропущенное в офлайне
  добирается у соседей при открытии темы (`mreq`). Карта кода -
  `docs/CHANNEL_SWARM_DESIGN.md` §9.4 «Этап 5», что проверить на
  телефонах - `AI_HANDOFF.md` 0g. Схема БД не менялась. На телефонах НЕ
  проверено.
- v11.70.10: `app-release.apk` = 37 153 078 байт, sha256 `07332541…0a69`,
  тег на коммите `f3d730d`, прогон 34543411775 с первого раза (рой, этап 4 -
  комментарии больших каналов через сборщиков, `AI_HANDOFF.md` 0f).
- v11.70.9: `app-release.apk` = 37 136 690 байт, sha256 `ddf73cfc…a93a`,
  тег на коммите `274b0b0`, прогон 34511780103 с первого раза.
  Описание - `docs/RELEASE_NOTES_v11.70.9.md`: короткие ссылки `/s/<код>`
  везде, где приложение делится ссылкой (карта кода - раздел «Ссылка
  пересылки поста» ниже). **Worker с короткими ссылками опубликован
  владельцем 2026-09-10 вечером**, проверен живьём: `/s/x` → «Ссылка
  неполная», `/short/<код>` → JSON, `/i?slug=…` и `assetlinks.json` как
  раньше. На телефонах НЕ проверено - что смотреть, в `AI_HANDOFF.md` 0e.
- v11.70.8: `app-release.apk` = 37 120 266 байт, sha256 `e0b7b1bf…1630`,
  тег на коммите `88766d4`, прогон 34430598604 с первого раза.
  Описание - `docs/RELEASE_NOTES_v11.70.8.md`: рой, этап 3 (Kotlin-часть:
  длинный текст кусками, счётчики через владельца на каналах > 20,
  эстафета умеющим рой) и запас под «+» в избранном/чатах/прокси. Карта
  кода - `docs/CHANNEL_SWARM_DESIGN.md` §9.4 «Этап 3». Схема БД не менялась
  (19). На телефоне НЕ проверен - что смотреть, в `AI_HANDOFF.md` 0d.
- v11.70.7: `app-release.apk` = 37 108 486 байт, sha256 `ec2fa082…1584`, тег на
  коммите `71d3f7c`, прогон 34385967858 с первого раза.
  Описание - `docs/RELEASE_NOTES_v11.70.7.md`: рой, этап 2 (первая волна
  K = 20, эстафета манифестов R = 10, куски полосами `pwant`, `peers` вместо
  полного списка при N > 100, 20 манифестов новичку, пределы отдачи) и
  исправление обрезанного фото у отправителя в личном чате. Карта кода -
  `docs/CHANNEL_SWARM_DESIGN.md` §9.4 «Этап 2». Схема БД не менялась (19).
  На телефоне НЕ проверен - что смотреть, в `AI_HANDOFF.md` 0c.
- v11.70.6: `app-release.apk` = 37 075 722 байт, sha256 `8862111e…48e9`,
  тег на коммите `5f10e13`.
  Описание - `docs/RELEASE_NOTES_v11.70.6.md`: рой, этап 1 - подписанные
  манифесты постов, досылка от любого подписчика, просьба трём соседям,
  схема БД 19. Прогон 34360928024 с первого раза (эта же сборка впервые
  скомпилировала `eddsa` и новые таблицы). На телефоне НЕ проверен - что
  смотреть, в `AI_HANDOFF.md` 0b.
- v11.70.5: sha256 `e4b465b7…fcb58`, коммит `c535800`, 2026-09-09: репост
  одним сообщением (картинка или сетка фото + подпись с текстом и ссылкой),
  режим «Без ограничений» в «Раздаче». Владелец проверил: «Репост лёгкий и с
  фото и с текстом и ссылкой. Всё переходит и работает.»
- v11.70.4: sha256 `9ada6eef…f2eb`, коммит `bc50494`, 2026-09-09: фото на
  весь экран со щипком (владелец: «всё хорошо»), рой кусок 1 («Раздача»
  есть), репост в два шага (владелец: «всё сложно» - заменён в v11.70.5).
  Первый тег (`a6946b1`) упал на константе в `SwarmSettings`, релиз не
  создался - тег переставлен на `bc50494`.
- v11.70.3: sha256 `c6c239c8…db0f`, коммит `357576c`, 2026-09-09 утром:
  репост поста с фото, «Избранное» хранит фото (схема БД 18). Владелец
  проверил: фото уходят, текст и ссылка - нет (исправлено в v11.70.4).
- v11.70.2: sha256 `5da8db75…8fb5`, коммит `9f6b414`, 2026-09-08: несколько
  фото в посте, правка поста, досылка старых постов, ANR-правка, «Назад» из
  поста. Владелец подтвердил только «6 фото прикрепляются»; остальное не
  проверено. Пуш тега тогда запустил ДВА прогона (34245362101 и
  34245370543) - второй лишь дописал в готовый релиз; ассет один.
- v11.70.1: sha256 `510d5997…24cd0`, коммит `79b129a`. Владелец проверил
  «Сообщества» и вход по ссылке на пост; нашёл ANR после публикации поста,
  возврат из поста в окно ссылки и «Настрой…» - всё исправлено в v11.70.2.
- v11.70.0: sha256 `8fe561f2…29eb9`, коммит `3a88e67`. Владелец подтвердил:
  окно обновления появилось у всех, пересланная ссылка на пост открывает APU
  и доводит до поста. Первый v11.70.0 (тег на отставшем `b7f7971`, схема БД
  15) удалён вместе с тегом до того, как его кто-либо получил - см. грабли
  в `AI_HANDOFF.md`.
- v11.69.3: sha256 `891d2f3b…4084a`, коммит `91916fb`.
- Обновление в приложении: `UpdateChecker` читает ТОЛЬКО GitHub
  `/releases/latest` при старте `MainActivity`; `/version` воркера
  (`meta:latest_version`) приложением не используется.
- Пустые релизы и теги v11.69.1/v11.69.2 удалены с разрешения владельца
  (2026-09-07). Релиз создаёт только Actions по тегу.
- **Ключ подписи раскрыт** (`p2p-release.jks` + пароль в публичном репо).
  Лечение подготовлено, но НЕ выполнено: `docs/SIGNING_KEY_ROTATION.md`,
  `scripts/rotate-signing-key.ps1`, `scripts/ci/build-release.yml`
  (владелец копирует в `.github/workflows` сам - боту туда пушить нельзя).
  **Живой workflow обновлён владельцем 2026-09-09** (коммиты `2909051` и
  `ae9076d` в `main`): `.github/workflows/build-release.yml` байт в байт
  равен `scripts/ci/build-release.yml` - шаг `Re-sign…` (без секретов -
  только предупреждение) и группа `concurrency` (один прогон на тег;
  подтверждено на v11.70.3).
- Кончик `main` проверять заново: `git ls-remote origin refs/heads/main`.
- **Размер APK ничего не доказывает** - у разных версий он совпадал до байта.
  Признак - `sha256` ассета и `built:` у локальной сборки.
- Канонический Windows-клон владельца: **`C:\APU-M8`**.

### Что сделано за сентябрьские раунды (79-95) и ЧТО ИЗ ЭТОГО ПРОВЕРЕНО

Подтверждено владельцем на телефонах:

- сквозное шифрование переписки (личные, группы, каналы, реакции, файловые
  пакеты) - `data/security/{MessageSealer,SealedWire}.kt`;
- две синие галочки «прочитано» - `data/receipt/`;
- реакции доходят в обе стороны;
- сердечки профиля (рейтинг) - `data/heart/`, счётчик и в своём профиле;
- защита личности: никнейм + пароль, вход на другом устройстве -
  `data/security/{IdentityVault,IdentityBackup}.kt`, хранилище на worker'е;
- короткие ссылки контактов `apu://a/...`;
- аватар: съёмка с камеры и выбор области - `ui/components/AvatarCropDialog.kt`;
- каналы: комментарии обычным чатом, посты с фото, копирование текста;
- v11.69.3: отступ под кнопкой «+» в ленте канала и кликабельная
  https-ссылка пересылки поста (владелец: «плюсик не мешает, ссылки активные»).
- v11.70.0 (2026-09-08): обновление пришло всем через окно в приложении;
  пересланная ссылка на пост «переходит по этапам, всё отлично» - значит,
  `assetlinks.json` + `autoVerify` работают, APU открывается сразу.

НЕ проверено на телефонах (ветка после v11.70.1):

- «Приложение не отвечает» каждые пять секунд после публикации поста
  (владелец, 2026-09-08, скриншот). Причина не в фото: `RustBridge.sendMessage`
  - блокирующий вызов ядра (прямой QUIC ждёт до 5 с на соединение и до 5 с
  на запись), а веер рассылки групп/каналов (`di/GroupsModule.kt`) звал его
  из `viewModelScope`, то есть с главного потока; `async` внутри
  `PerMemberFanoutDelivery` наследует диспетчер вызывающего. Пост = пакет
  темы + пакет сообщения, до 10 с на каждого недоступного подписчика с
  известным адресом. Тот же дефект, что у ACK'ов в `5f713d7`. Правка: веер
  шлёт в `withContext(Dispatchers.IO)` (одна точка на все ~20 рассылок),
  личная отправка в `ChatRepository.sendMessage` и запрос реферального
  токена в `AddContactViewModel` - тоже в IO; `RustBridge.sendMessage`
  пишет в logcat предупреждение с трассой, если его всё же позвали с
  главного потока. Правило: `RustBridge.sendMessage` - только с IO;
- «Назад» из поста, открытого по ссылке, ведёт в ленту канала, а не обратно
  в «Сообщества» с повторным окном «Вы подписаны…» (владелец, 2026-09-08,
  скриншоты). Два дела: `NavGraph.onOpenPost` кладёт под пост маршрут
  `Screen.Channel`, а `GroupsScreen` входит по `joinLink` один раз за жизнь
  экрана (`joinHandled` в `rememberSaveable`, как `createHandled`);
- нижняя панель: кнопкам поровну ширины и один кегль подписи на всех,
  посчитанный по самой длинной (`ApuBottomBar`, `rememberTextMeasurer`).
  На v11.70.1 «Сообщества» отобрало место, и «Настройки» стало «Настрой…»
  - расчёт по метрикам Roboto был верен для 412dp, а у владельца экран
  375dp. Урок: считать под 360dp, не под свой телефон;
- посты канала (владелец, 2026-09-08, три пожелания к одному релизу):
  **до 6 фото в посте** - каждое фото режется на куски по 3200 символов
  base64 и уходит отдельными сообщениями темы (`APUIMGP1:<фото>/<кусок>/
  <всего>:…`), потому что через публичный MQTT-брокер не проходит ничего
  больше ~10 КБ (старый пост с одним фото на 7000 символов ходил только по
  Wi-Fi/QUIC); лента склеивает куски в галерею (`PostGallery`,
  `HorizontalPager`), в комментариях и счётчиках их нет. **Правка поста**
  автором или владельцем канала (`GroupWire.KIND_EDIT`, карандаш в углу
  карточки; фото при правке не меняются). **Старые посты опоздавшим**:
  владелец при приёме подписчика и по запросу `KIND_POSTS_REQUEST`
  досылает последние 20 постов с фото, в конверте сообщения появились
  автор и исходное время (поля 8-9, только у досылки; телефоны с прошлой
  версией такой конверт молча отбрасывают - у них старых постов не будет).
  Подробности - `AI_COLLABORATION_NOTES.md`, раунд 99. Выпущено в
  v11.70.2 (владелец передумал ждать проверки: «выпускай релиз для всех»).
  Владелец подтвердил на телефоне: 6 фото прикрепляются. Остальное из
  v11.70.2 ещё не проверено.

Выпущено в v11.70.3 (2026-09-09, на телефонах не проверено):

- **репост поста с фотографиями** (владелец, 2026-09-08: «репост должен
  идти с фото, а не только текст»). `ChannelViewModel.sharePost` пишет
  jpeg-и поста в `cache/shared` и отдаёт системному меню через FileProvider
  (`ACTION_SEND`/`ACTION_SEND_MULTIPLE`, права через ClipData; помощник -
  `util/PhotoShare.kt`), текст + ссылка «Открыть в APU» идут подписью.
  «В избранное» теперь хранит фото поста (колонка `photos` в `saved_items`,
  **схема БД 18**, `MIGRATION_17_18`), экран «Избранное» показывает их и
  умеет поделиться дальше тоже с фото; сообщение группы с картинкой
  сохраняется как текст + фото, а не «буквами». Тест `SavedItemPhotosTest`.
  В песочнице не компилировалось, но CI собрал APK без ошибок.

Что вошло в v11.70.8 (рой, этап 3, Kotlin-часть):

- **длинный текст кусками.** Текст поста/сообщения темы длиннее ~3 КБ
  режется на куски (`InlineImage.splitText`, ≤ 3 100 байт UTF-8, до 8
  кусков) и едет отдельными сообщениями темы под маркером кусков фото с
  номером фото 0 (`APUIMGP1:0/…`) - старые телефоны их прячут и фото не
  считают. Голова несёт первый кусок и пометку `APUIMGP1:0/0/<n>:<правка>`.
  Куски входят в манифест поста и раздаются роем как фото. Правка длинного
  текста - с номером правки, старые куски удаляются у всех. Тест
  `InlineTextPartsTest`.
- **счётчики через владельца.** На канале > 20 подписчиков просмотры и
  реакции идут только владельцу и администраторам; читатель при открытии
  канала спрашивает сводку `pcreq` → `pcnt` (`PostCounterRepository`,
  `SwarmPolicy.countersViaHubs`). Сводка ложится в те же таблицы служебными
  строками `hub:…`. Тесты в `GroupWireTest`.
- **эстафета умеющим рой.** `onPostComplete`/`publishRoster` сначала берут
  соседей с известным ключом (`allSelfAttestedIds`).
- **«+» не закрывает последнюю запись** в избранном, списке чатов и списке
  прокси (`contentPadding` bottom = 88.dp).
- Не сделано (ядро): подпись в ядре, куски по QUIC - uniffi-обвязка не
  перегенерируется в CI. Без компилятора; на телефонах не проверено.

Что вошло в v11.70.7 (рой, этап 2):

- **большие каналы без веера владельца.** Пока умеющих рой подписчиков
  (предъявивших свой ключ `pkeys`) не больше 20, всё как в v11.70.6. Больше -
  автор шлёт пост целиком только старым телефонам и 20 лучшим по ярусу,
  манифест - этим 20; каждый собравший пост через 1–5 с передаёт манифест
  10 соседям (эстафета), а получивший манифест без поста тянет куски
  полосами (`pwant k/m`, m ≤ 4) у тех, от кого пришли манифест или куски,
  три раунда по 10 с. Новичку большого канала владелец шлёт 20 манифестов,
  а не куски; при N > 100 вместо полного `roster` - `peers` (число
  подписчиков + 30 соседей) и короткий список администраторов. Отдача
  ограничена: 3 полосы разом, 60 кусков в минуту. Новые виды пакетов
  `pwant` и `peers` старые телефоны молча отбрасывают. Файлы: `GroupWire`
  (`pwant`/`peers`/`stripe`), `GroupRepository` (раздел «Рой, этап 2»),
  `PostSignerDao.allSelfAttestedIds`, `GroupDao.bumpMemberCount`. Тесты -
  `GroupWireTest` (полосы, `pwant`, `peers`). Подробно -
  `docs/CHANNEL_SWARM_DESIGN.md` §9.4. Без компилятора; на телефонах не
  проверено.
- **фото в личном чате у отправителя целиком.** Владелец 2026-09-09: «у
  отправителя если зайти на фото, то она сверху и снизу обрезана». Причина:
  превью исходящей картинки (`file_preview/v1/<transferId>.jpg`) делал
  `AvatarCompress.compressUri` - центральный квадрат 320×320 (это правильно
  для аватара, но не для фото), а просмотрщик у отправителя открывает именно
  это превью (оригинал остаётся в галерее по URI, куда после отправки доступа
  может не быть). Теперь `util/PhotoPreview.kt` пишет уменьшенную копию с
  теми же пропорциями (длинная сторона ≤ 1280, поворот по EXIF), а пузырь
  (`FileTransferBubble`) показывает картинку по её пропорциям (`Fit` +
  `aspectRatio`, не выше 320 dp) вместо `Crop` по ширине. Уже отправленные
  ранее фото останутся квадратными - их превью записано старым кодом.

Что вошло в v11.70.6 (рой, этап 1):

- **посты канала раздают сами подписчики.** После каждого поста владелец
  или администратор шлёт подписанный манифест `pman` (Ed25519 ключом
  личности через Java-библиотеку `net.i2p.crypto:eddsa:0.3.0`; семя то же,
  что в ядре - сверяется с `identitySigningPublicKeyHex()` перед первой
  подписью). Манифест = хэш текста + id и sha16 каждого куска фото + номер
  правки. Получатель, у которого он есть, принимает пост и куски **от любого
  участника**, а не только от владельца, и сам отвечает на `preq` (только
  посты с манифестом и полным набором кусков, ≤ 6 просьб в минуту, только
  участникам из своей таблицы). Просьба о постах уходит владельцу и ещё 3
  соседям (случайно из 10 лучших по ярусу), недостающие посты делятся между
  соседями полосами. Ключи подписи: узел предъявляет свой сам (в `pman`
  или `pkeys`), владелец заверяет ключи администраторов (`pkeys` новичку
  при вступлении и по `pkreq`). Правка поста автором → новый манифест с
  `revision + 1`. **Схема БД 19**: `post_manifests`, `post_signers`
  (`MIGRATION_18_19`). Файлы: `data/swarm/PostManifest.kt`, `PostSigner.kt`,
  `SwarmBuffer.kt`, `GroupWire` (`pman`/`pkeys`/`pkreq`), `GroupRepository`
  (необязательные зависимости конструктора - старые JVM-тесты не тронуты),
  `GroupsModule`. Тесты `PostManifestTest` (вектора RFC 8032 §7.1 -
  доказательство совместимости с ed25519-dalek ядра), `SwarmBufferTest`.
  Полная карта - `docs/CHANNEL_SWARM_DESIGN.md` §9.4. В песочнице не
  компилировалось; на телефонах не проверено. Старые версии приложения
  новые виды пакетов молча отбрасывают и продолжают получать посты от
  владельца по-старому.

Что вошло в v11.70.4 и v11.70.5 (после v11.70.3):

- **фото на весь экран с увеличением** (владелец, 2026-09-09: «на фото
  нажимаешь - и фото на весь экран, увеличивать двумя пальцами»).
  `ui/components/PhotoViewer.kt`: `Dialog` во весь экран, `HorizontalPager`
  по фото поста, свой разбор жестов (`awaitEachGesture` в Initial-проходе:
  щипок 1..6×, сдвиг в границах картинки, при масштабе 1 горизонтальный
  сдвиг уходит пейджеру), двойное нажатие 2.5× в точку, одно - шапка.
  Источник - `PhotoSource.Encoded(base64)` или `PhotoSource.File(path)`.
  Подключено: `PostGallery` (канал, одно фото и галерея), `SavedScreen`
  (фото поста и картинка-файл), `GroupChatScreen` (картинка в
  комментарии/группе), `FileTransferBubble` (присланное фото в чате).
  Картинки-ссылки (Coil) не подключены. Не проверено на телефоне;
- **репост одним сообщением (v11.70.5).** Владелец о двух шагах v11.70.4:
  «всё сложно… нажал поделиться - и текст, и ссылки, и фото всё сразу».
  Факт из исходников Telegram (`LaunchActivity`): подпись (EXTRA_TEXT ≤ 1024)
  принимается только при ACTION_SEND с ОДНИМ файлом; при ACTION_SEND_MULTIPLE
  текст не читается вовсе. Поэтому `util/PhotoShare.writeShareImage`: одно
  фото - как есть, несколько - одна картинка-сетка `util/PhotoCollage`
  (1080 px, ряды 2/2+1/2+2/3+2/3+3, клетки 3:4 или 4:3 по большинству фото,
  центр-кроп), и `buildImageIntent(uri, caption)` - ACTION_SEND image/jpeg +
  EXTRA_TEXT. Подпись `captionFor` ≤ 1000 знаков, режется текст, ссылка
  остаётся. `SharePostDialog`/`PendingShare`/`shareText`/`sharePhotos`
  удалены. Тест `PhotoCollageTest` (раскладка, подпись). Не проверено на
  телефоне;
- **режим «Без ограничений»** в «Раздаче» (предложение владельца):
  `SwarmMode.UNLIMITED` = 32 одновременно / 3000 пакетов в минуту, без
  скидок на мобильный интернет и заряд; хранится `swarm_mode=unlimited`;
- **репост с фото: текст и ссылка терялись** (v11.70.4, два шага - заменено
  выше). Владелец проверил v11.70.3:
  «фото пересылаются, а остального ничего нет - ни текста, ни ссылки».
  Причина: мессенджеры (Telegram точно) при `ACTION_SEND_MULTIPLE`
  выбрасывают `EXTRA_TEXT`. Лечение: две отправки - окно
  `ui/components/SharePostDialog.kt` (шаг 1 текст со ссылкой `text/plain`,
  шаг 2 фото), состояние `PendingShare` в `ChannelViewModel` и
  `SavedViewModel`; текст заодно кладётся в буфер обмена
  (`PhotoShare.copyToClipboard`). `PhotoShare.buildIntent` разделён на
  `buildTextIntent`/`buildPhotosIntent`. Не проверено на телефоне;
- **рой, кусок 1: приоритеты и пределы.** Владелец 2026-09-09: «всё, что
  роем можно сделать, - всю систему настраивать на ройную», сначала свои,
  проверенные, популярные, стабильные - потом все подряд, и предел, чтобы
  телефон не перегружался. Сделано: пакет `data/swarm/` (`SwarmPolicy` -
  ярусы и пределы, `SwarmBudget` - ведро жетонов на две полосы,
  `SwarmPeerDirectory` - кто свой/проверенный/стабильный из базы и
  рейтинга, `SwarmSettings` - режим + мобильный интернет/заряд); все
  групповые вееры идут через него (`PerMemberFanoutDelivery`: порядок по
  ярусу, ширина и жетон перед каждой отправкой), просмотры и реакции - по
  служебной полосе с обрезкой; в настройках раздел «Раздача» (Обычный
  300/мин, Экономный 120/мин). Тест `SwarmPolicyTest`. Не компилировалось
  (нет Kotlin в песочнице), на телефонах не проверено. Подробности -
  `docs/CHANNEL_SWARM_DESIGN.md`, раздел 9. Этап 1 (манифест поста,
  `preq` любому сиду) - в v11.70.6, см. выше.

Выпущено в v11.70.1 (2026-09-08), владелец проверил «Сообщества» и вход по
ссылке на пост (работает; см. замечания выше):

- слова первого окна по ссылке: для поста - «Открыть пост?», для канала -
  «Подписаться на канал?», для группы - прежнее «Войти в группу?»
  (`data/group/InvitePrompt.kt`, тест `InvitePromptTest`). Владелец на
  v11.70.0: ссылка на пост спрашивала «Войти в группу?» - «нужно чтобы не
  путал людей». Заодно итоговое окно на экране «Группы» говорит «подписаны на
  канал», а не «вошли в канал»;
- нижняя вкладка и раздел называются «Сообщества» вместо «Группы»: в них
  живут и группы, и каналы (владелец, 2026-09-08, по скриншоту: «нужно как-то
  назвать правильно или разделить»; разделять не стали - в поиске и списках
  тип и так подписан под названием). Заодно слова для каналов везде про
  подписку: кнопка каталога «Подписаться», выход - «Отписаться от канала?»,
  удаление - «у всех подписчиков» (`ApuMainTabs`, `GroupsScreen`,
  `ChatListScreen`, `GroupAdminScreen`). В списке чатов фильтр «Группы»
  остался - там он фильтрует именно группы;
- вставка ВСЕГО пересланного сообщения в «Войти по ссылке» (раньше веб-форму
  ссылки регулярка поиска не знала - весь текст отвергался);
- нажатие на нашу ссылку внутри чата APU открывает её в APU, минуя браузер.

### Ссылка пересылки поста: как устроена и что где лежит

- **С v11.70.9 наружу уходит КОРОТКАЯ ссылка** `https://<хост>/s/<код>`
  (10 знаков, ни канала, ни владельца, ни записи в адресе - просьба
  владельца 2026-09-10). Код выдаёт worker (`POST /short`, KV `short:<код>`,
  код = SHA-256 полной ссылки `p2pmessenger://group?…`, поэтому один и тот
  же пост всегда даёт один код); приложение разворачивает код через
  `GET /short/<код>`; браузер без APU получает страницу `GET /s/<код>`.
  Kotlin: `data/link/ShortLinks.kt` (разбор, чистый Kotlin, тест
  `ShortLinksTest`), `data/link/LinkShortener.kt` (кэш кодов + пауза 2 мин
  после отказа сервиса), `data/link/ShortShare.kt` («Поделиться» для
  экранов без ViewModel), `BotApi.shortenLink/expandShortLink` (таймаут
  5 с), `GroupRepository.shareLinkFor/expandLink` (хуки приходят из
  `GroupsModule`; в JVM-тестах их нет - ссылки длинные).
- Где сокращается: репост поста (`postLinkFor` - теперь одно бессрочное
  приглашение «вход сразу» на канал, `openInviteFor`, а не новое на каждый
  репост), «Поделиться» группой/каналом из «Сообществ» и списка чатов
  (`inviteLinkFor`), несколько групп контакту (`inviteLinksFor`), список
  ссылок в админке (`InviteSummary.shareLink`, QR - по основной ссылке),
  свой профиль (`ShortShare.shareInvite` в контактах/QR/рангах,
  `ShareProfileViewModel.alternativeLink`), контакт из списка
  (`ShortShare.shareText`).
- Где разворачивается: `MainActivity.resolveShortLink` (тап снаружи),
  сканер QR в `NavGraph`, «Войти по ссылке» (`GroupsViewModel.joinByLink`
  → `expandLink`), `AddContactViewModel` (вставка). Сервис не ответил -
  прямое сообщение «нет связи с сервисом APU», а не «это не ссылка».
- Worker опубликован 2026-09-10 вечером; без него (или без сети) приложение
  делится длинной ссылкой - это штатный откат, а не ошибка.
- Длинный вид остаётся: строит `GroupInviteLinks.buildWebLink` →
  `https://p2p-relay.1985vzhem.workers.dev/i?slug=…&g=…&o=…&p=…&c=1`.
- Понимает: `GroupInviteLinks.parseTarget` (и чистый адрес, и внутри текста -
  `LINK_PATTERN`), `MainActivity.handleDeepLinkIntent`, intent-filter на хост
  `p2p-relay.1985vzhem.workers.dev` пути `/i` и `/s/` в манифесте.
- Хост зашит в трёх местах: `GroupInviteLinks.WEB_HOST`, манифест, worker.
  Менять только вместе. Хост `apumir.app` уже принимается везде
  (`OFFICIAL_HOST`, манифест) - когда владелец купит домен и подключит его к
  worker'у как Custom Domain, достаточно сменить `WEB_HOST`.
- Worker (`tools/worker/p2p_relay_worker.js`): `/i` - страница «Открыть в
  APU / Установить», `/s/<код>` - та же страница по короткой ссылке,
  `POST /short` и `GET /short/<код>` - короткие ссылки (v11.70.9),
  `/.well-known/assetlinks.json` - отпечатки ключей подписи
  релиза (массив `RELEASE_CERT_SHA256S`; сейчас один, `F8:43:CB:E7…A5:F7`;
  после ротации ключа добавить второй). Версия с `/i` и `assetlinks`
  **опубликована владельцем 2026-09-07 вечером**, проверена живьём.
  Версия с короткими ссылками **опубликована владельцем 2026-09-10
  вечером** (первая вставка оборвалась на 357-й строке из 478 - «Unexpected
  end of input»; файл переслан двумя кусками). Признак новой версии:
  `https://p2p-relay.1985vzhem.workers.dev/s/x` отвечает «Ссылка неполная»
  (старый worker отвечал JSON `not found`). Порядок для будущих правок тот
  же: сначала worker, потом APK - Android проверяет файл при установке
  приложения.
- Тесты: `GroupInviteLinksTest` (`web link …`). Slug в тестах - только из
  алфавита `newSlug` (без `0 1 I O l`), иначе `isValidSlug` тихо отбракует и
  тест проверит пустоту - так и случилось с `"abcd1234"` в v11.69.x.

### Хранилище личности на сервере (worker)

Владелец развернул два обработчика в своём Cloudflare Worker
(`p2p-relay.1985vzhem.workers.dev`): `/vault/put` и `/vault/get`. Проверены
живьём, отвечают. Полный код worker'а и пошаговая инструкция -
`tools/worker/p2p_relay_worker.js` и `docs/IDENTITY_VAULT_SETUP.md`.
Потом в файл добавлены `/i` (страница «Открыть в APU / Установить») и
`/.well-known/assetlinks.json` - **опубликованы владельцем 2026-09-07**.
Публикация любой следующей правки - шаг 3 из `docs/IDENTITY_VAULT_SETUP.md`
(скопировать файл целиком, Deploy); проверка - открыть в браузере
`https://p2p-relay.1985vzhem.workers.dev/.well-known/assetlinks.json`.
Грабли: владелец ищет файл в `C:\APU-M8`, а там `main` - правки из ветки
сессии туда попадают только после `sync-main.ps1`; до этого присылать код
worker'а прямо в чат целиком.

## 4. Первые три шага нового чата (обязательно, до задачи)

1. **Выровнять песочницу по `main`.** Ориентир один — `main` (раздел 9);
   ветка прошлого чата не нужна. Если песочница ещё не склонирована:
   `git clone https://github.com/vzhem/APUMIR <папка>` — обычный clone даёт
   ветку `main`, полную историю и все теги (на 2026-08-30 их 52).
   Если песочница Arena уже открыта, она приходит **обрезанной и
   одноветочной**, поэтому порядок такой:
   `git config remote.origin.fetch '+refs/heads/*:refs/remotes/origin/*'` →
   `git fetch origin --prune` → `git fetch --tags origin` →
   `git fetch --unshallow origin main` (если `git rev-parse
   --is-shallow-repository` печатает `true`) → `git reset --hard origin/main`
   (если в рабочем столе нет неприкаянных правок; если есть — сначала
   `git reset -q origin/main` и посмотри `git status`).
   Кончик всегда бери из `git ls-remote origin refs/heads/main`, а не из этого
   файла — текст устаревает. Правда о сервере — `git ls-remote origin`:
   в одноветочном клоне `git fetch origin <не-main>` не создаёт
   `origin/<ветка>`, а пишет только в `FETCH_HEAD`.
2. **Прочитать** раздел 2 и в первом ответе владельцу коротко назвать:
   последнюю выпущенную версию, кончик ветки, что доказано на телефонах,
   что НЕ доказано.
3. **НЕ начинать код по своему почину.** Владелец сам даст серьёзную задачу.
   До неё — только чтение и короткий честный доклад.

## 5. Правила владельца (нарушать нельзя)

- Один маленький шаг: правка → проверка → следующий шаг. Проект не
  перезапускать, не переписывать «с нуля».
- По-русски и простыми словами; объяснять, какие этапы «молчат» и как
  проверить живость.
- **Телефоны не трогать** без явного предупреждения и поимённо. Устройства
  (серийники встречаются в `docs/AI_HANDOFF.md`, `docs/MASTER_PLAN_v2.md` и
  журнале): Стас `11567254BK001192`, Аня `AUYF6R5923006121`,
  Женя `3B665800EES00000`. Командами на телефоне занимается сам владелец.
- **Команды для Windows (правило от 2026-08-29, усилено после краша).** Каждая
  команда даётся В ОТДЕЛЬНОМ блоке кода из ОДНОЙ строки (```` ```powershell ````
  + одна строка), чтобы владелец жал «скопировать» и не тянул лишнего.
  Строка всегда начинается со слова `powershell` и рассчитана на НОВОЕ окно
  PowerShell: `powershell -NoProfile -ExecutionPolicy Bypass -File
  C:\APU-M8\scripts\<имя>.ps1` либо `powershell -NoProfile -ExecutionPolicy
  Bypass -Command "<одна команда git с -C C:\APU-M8>"`. Пути — абсолютные,
  от текущего каталога не зависеть. Никогда не давать: многострочные блоки
  (были парс-краши), команду с опечаткой (даже с исправлением в том же
  сообщении), команду, где первое слово можно потерять при копировании.
  Если у владельца вылетело `-NoProfile : Имя "-NoProfile" не распознано...` —
  это значит, что при копировании потерялось слово `powershell`; сказать ему
  «закройте окно, откройте новое и вставьте команду целиком», а не давать
  команду заново в другом виде.
- **В командах владельцу НЕ использовать `$`-переменные** (ошибка агента
  2026-08-29): строка `-Command "... if ($LASTEXITCODE -eq 0) ..."` до git не
  дошла — ВНЕШНИЙ PowerShell подставил `$LASTEXITCODE` пустым местом, и
  получилось `if ( -eq 0)` с ошибкой `-eq : Имя "-eq" не распознано...`.
  Команда владельцу = ОДИН вызов git/программы без `$`, без `if`, без `;`.
  Проверку «входит ли коммит в main» делать идиомой без переменных:
  `git merge-base <коммит> origin/main` — если напечатан тот же хеш, что
  спрашивали, коммит внутри `main`; если другой — НЕ внутри.
  (`git rev-list --count <коммит>..origin/main` для этого НЕ годится: он всегда
  печатает число больше нуля.)
- **Никогда не давать владельцу `merge` без `fetch`** (ошибка агента
  2026-08-29, дважды): `git merge --ff-only origin/main` двигает только до того
  `origin/main`, который УЖЕ скачан, поэтому новый файл/скрипт до владельца не
  доезжает, и следующая команда падает с `Аргумент ... для параметра -File не
  существует`. Варианты: либо две команды (`fetch`, потом `merge`), либо одна
  `git -C C:\APU-M8 pull --ff-only` (она сама делает fetch). И наоборот: если
  файл нужен СЕЙЧАС, а сети нет — не гонять владельца за скриптом, а дать
  разовые команды чистым git.
- git без пайплайнов и без `2>$null`; перед КАЖДЫМ коммитом `git log --oneline -1`;
  при рассинхроне fetch + reset. Коммиты/пуш своей arena-ветки — без спроса;
  **релиз/тег — только с явного разрешения**.
- `.github/workflows/*` не пушить и не менять.
- Юнит-тесты не смешивать с assemble в одном `--tests`; «зелёный фильтр» ≠ гейт.
- Грубый brace-чекер врёт — есть лексер `tools/sandbox/struct_check.py`
  (полная версия; короткие ранние копии лежат в `tools/` и не являются
  эталоном).
- Свежесть сборки на телефоне смотреть по `dumpsys lastUpdateTime`;
  установка только `adb install -r -t -d`.
- Непроверенное на экране называть непроверенным. Имя приложения — «APU».
- **Резервная копия — после каждого релиза** (уточнение владельца
  2026-08-29): как только релиз выпущен (или вот-вот будет выпущен), агент сам
  предлагает владельцу «вставьте белую флешку (метка `APU_BACKUP`)» и одну
  команду (**powershell**):
  `powershell -NoProfile -ExecutionPolicy Bypass -File C:\APU-M8\scripts\backup-to-usb.ps1`.
  Скрипт найдёт флешку по метке сам. Для быстрой копии рабочего дерева годится
  и `scripts\backup-to-flash.ps1`; чем они отличаются — в разделе 3.
  После мелких промежуточных правок предлагать не обязательно — владелец сам
  скажет, когда обновить.

## 6. Грабли, на которые уже наступили (не повторять)

- **Песочница отстаёт от origin** (2026-08-29: локальный HEAD висел на
  `f0ca016`, пока origin был на `39d4557`, а весь код раундов 20–46 лежал в
  рабочем столе неотреслеженным). Сначала fetch/reset, потом выводы.
- **Документация расходилась с реальностью** (найдено 2026-08-30):
  `NEXT_AI_CHAT_BOOTSTRAP.md` называл себя готовым prompt для нового чата,
  описывая состояние на 2026-08-22, а `AI_HANDOFF.md` писал «сейчас v11.23.0».
  Первый стал заглушкой, второй сужен до правил, телефонов и незакрытых задач.
  Версию и коммит в любом случае сверять с `git ls-remote origin` и
  `/releases/latest`, а не с текстом документа.
- **Скопированная команда потеряла первое слово** (2026-08-29): владелец вставил
  в PowerShell строку без начального `powershell`, вышло
  `-NoProfile : Имя "-NoProfile" не распознано как имя командлета...`.
  Лечение — давать каждую команду отдельным блоком кода из одной строки
  (правило в разделе 5) и просить вставить её в НОВОЕ окно целиком.
- **Новый скрипт не найдётся, пока клон не переведён на новый `main`**
  (2026-08-29): владелец получил
  `Аргумент "C:\APU-M8\scripts\sync-main.ps1" для параметра -File не существует`
  — рабочий стол был на старом коммите `39d4557`, а скрипт появился только в
  `9fab4ae`. Порядок всегда такой: fetch → `git checkout main` →
  `git merge --ff-only origin/main` → и только потом запуск нового скрипта.
- **`! [rejected] <тег> (would clobber existing tag)`** при `fetch --tags`
  (2026-08-29, тег `v11.17.1`): локальный тег — другой объект, чем в origin.
  Не лечится силовым fetch вслепую. Смотреть надо КОММИТЫ, а не объекты:
  `git rev-list -n 1 v11.17.1` и `git ls-remote origin "refs/tags/v11.17.1*"`
  (без `^{}` — см. следующую граблю). У владельца вышло: локальный тег на
  `c1dfd82` («ci: build Rust from source in release workflow», 21.08 13:10),
  а в origin — на `6deb532` («ci: robust native build», 21.08 13:30), то есть
  на 2 коммита позже; `c1dfd82` входит в `6deb532`, оба входят в `main`,
  опубликован APK именно с `6deb532` (релиз `APU v11.17.1`,
  `app-release.apk` 35 285 255 байт). Лечение — `scripts/fix-tags.ps1`:
  он приводит ЛОКАЛЬНЫЕ теги к origin, проверяя достижимость, и не трогает
  теги, которых в origin нет.
- **`^{}` в строке `-Command "..."` ломает PowerShell** (2026-08-29): владелец
  получил `git.exe : ScriptBlock следует указывать только в качестве значения
  для параметра Command` — фигурные скобки PowerShell читает как блок кода.
  В командах для владельца синтаксис `v11.17.1^{commit}` и
  `refs/tags/v11.17.1^{}` НЕ использовать. Замена: `git rev-list -n 1 <тег>`
  (коммит из тега) и `git ls-remote origin "refs/tags/<тег>*"` (обе строки).
- **Сеть на ПК владельца рвётся** (2026-08-29): `sync-main.ps1` упал на
  `fatal: unable to access ... Failed to connect to github.com:443 after
  21110 ms` и напечатал `RESULT: fetch failed.`, ничего не двигая. Так и
  задумано: без свежего fetch скрипт работать не будет. Лечение — повторить.
- **Сборочные скрипты: сначала узнать версию инструмента** (ошибка агента
  2026-08-29): правка `android-app/app/build.gradle.kts` с `exec { }` уронила
  гейт — `Unresolved reference 'exec'`, потому что проект на Gradle 9.5.0
  (`android-app/gradle/wrapper/gradle-wrapper.properties`), а `Project.exec`
  удалён в Gradle 9 (deprecated с 8.11). Для запуска внешних команд в
  конфигурации брать `java.lang.ProcessBuilder` или `providers.exec`, и
  сначала смотреть версию Gradle/AGP/Kotlin в wrapper и `libs.versions.toml`.
  Правило шире: имена API сверять с версией инструмента в РЕПОЗИТОРИИ, а не по
  памяти (та же грабля была с Compose foundation 1.7.6).
- **В `.kts` полное имя JDK безопасно только в позиции ТИПА** (ошибка агента
  2026-08-29, вторая подряд на том же файле): `java.util.concurrent.TimeUnit`
  в выражении дал `Unresolved reference 'util'`, потому что `java` в скрипте
  Gradle — это accessor расширения `JavaPluginExtension`, а не пакет.
  `ProcessBuilder(...)` и `catch (e: Exception)` при этом компилируются:
  там `java...` стоит в позиции типа. Для значений — `import` в начале файла
  (до блока `plugins`) либо обходиться без такого имени вовсе.
- Непроверенную правку сборочного файла в `main` не отправлять: кандидат
  живёт в ветке сессии, владелец прогоняет гейт на нём (`git checkout --detach
  FETCH_HEAD`), и только после GREEN правка уходит в `main`.
- PowerShell 5.1: `$ErrorActionPreference='Stop'` + stderr от `gh`
  («release not found», пока Actions строит) убивает скрипт. В циклах ожидания
  вызывать gh через `cmd /c "gh ... 2>nul"`.
- `gh` определяет репозиторий по текущей папке: в скриптах
  `Push-Location 'C:\APU-M8'`, в разовых командах владельца `-R vzhem/APUMIR`.
- Сеть на ПК владельца нестабильна: promote может оборваться на середине.
  Тогда добить вручную:
  `gh release edit <tag> -R vzhem/APUMIR --draft=true` и следом `--draft=false`.
- Снять prerelease НЕДОСТАТОЧНО: `/releases/latest` двигается только через
  цикл draft → publish.
- Длинные команды с путями при копировании владельцем искажаются — такие вещи
  заворачивать в `.ps1`, а не давать строкой.
- Не давать команды копирования на диск вслепую: сначала `Get-Volume`,
  потом путь с реальной буквой (скрипт ищет флешку по метке).
- Compose foundation 1.7.6: `combinedClickable` — `@ExperimentalFoundationApi`
  (`@OptIn` + импорт); `clip` требует `import androidx.compose.ui.draw.clip`;
  `LineLimits` НЕ существует; приём контента с клавиатуры работает только в
  `BasicTextField(state = TextFieldState)` + `Modifier.contentReceiver`.
- `MessengerColors.of(...)` / `colors.gold` / `ic_launcher_foreground` — таких
  сущностей нет, не выдумывать.
- `git checkout -- <файл>` НЕ отменяет правку, которая уже в индексе
  (а `git checkout <commit> -- <файл>` кладёт её именно в индекс: статус
  показывает `M ` в ПЕРВОМ столбце). Команда выглядит выполненной, файл
  остаётся изменённым, `git pull --ff-only` падает с «Your local changes ...
  would be overwritten by merge» — и гейт собирает СТАРЫЙ код, сообщая GREEN.
  Правильно: `git restore --source=HEAD --staged --worktree -- <файл>` либо
  `git reset --hard HEAD`, и только потом `git pull`. Проверено воспроизведением
  в песочнице 2026-08-29 (см. AI_COLLABORATION_NOTES, раунд 50, продолжение).
- **`--tests` в гейте — это ФИЛЬТР, а не отчёт** (ошибка агента 2026-08-29):
  `groups-build-gate.ps1` запускает unit-тесты с явным списком пакетов, поэтому
  новый тест-класс вне списка компилируется и молча не выполняется, а итоги
  остаются прежними и выглядят зелёными. Так уехали `ReferralWireTest` и
  `ReferralCreditPolicyTest` — гейт показал те же 108 тестов. Новый тест-пакет
  добавляется в `--tests` в том же коммите; гейт печатает NOTE со списком
  непокрытых классов.
- Зелёный гейт ещё не значит, что собран нужный код: если Gradle пишет
  «N actionable tasks: N up-to-date», а дерево грязное — собирался прежний
  файл. Сверять `Repo HEAD` в шапке гейта и `versionName` в
  `install-debug-on-phones.ps1`.
- Гейт `scripts/groups-build-gate.ps1` при провале удаляет APK; после правок,
  задевших kapt/Room, запускать с `-Clean`.
- Release-APK подписан другим ключом — поверх debug не ставится.

## 7. Как устроен выпуск релиза (проверено на v11.23.0)

1. `scripts/make-release.ps1 -Version vX.Y.Z -ExpectedCommit <sha>` —
   проверяет чистоту дерева, гоняет гейт, ставит тег и пушит.
2. GitHub Actions (`.github/workflows/build-release.yml`) собирает подписанный
   `app-release.apk` (~12–20 мин) и публикует **PRERELEASE** с body-заглушкой.
3. `scripts/promote-release.ps1 -Version vX.Y.Z` — ждёт релиз с APK, снимает
   prerelease, делает draft → publish, сверяет `/releases/latest`.
4. `gh release edit vX.Y.Z -R vzhem/APUMIR --notes-file docs\RELEASE_NOTES_<версия>.md`
   — краткие заметки (их видит окно обновления в приложении).
5. **Догнать `main`** (см. раздел 9): рабочий кончик должен попасть в `main`
   в тот же день, иначе `main` снова превращается в мёртвую ветку.

## 8. Что доказано и что нет (честно)

**Доказано на реальных телефонах:** передача файла 64 МБ напрямую
телефон→телефон по общему Wi-Fi (LAN-first: discovery-порт 42108, кадр
`APULAN1|iam|<nodeId>`, скан /24 без MQTT-брокера, конвейерный приём кадров).
Работают разделы ГРУППЫ/КАНАЛЫ, аватары (свои, 50 стандартных, групп/каналов),
стикеры и картинки с клавиатуры **в личных чатах** (с ранга 3), полноценные фото
в пузыре с меню «Сохранить/Поделиться», темы день/ночь + обои, окно обновления.

**НЕ доказано / не сделано:** стикеры и файлы в группах и каналах (в
`GroupChatViewModel` нет файла-трубы; вложения в группах не включать до
mesh-кастодии — `rust-core/src/file_custody.rs` написан, но к движку/FFI/Android
не подключён); офлайн-информирование об удалении группы; тап по уведомлению
группы; короткие ссылки (нужен сервис-резолвер); меню картинки только для
INCOMING COMPLETE; контрольный замер скорости файловой передачи после всех
правок не проводился.

## 9. Как правильно устроен репозиторий на GitHub (правило от 2026-08-29)

**Что должно быть:**

- `main` — ЕДИНСТВЕННАЯ ветка правды. Это «то, что сейчас у людей в телефонах
  плюс документация». Она обязана совпадать с рабочим кончиком (или отставать
  максимум на текущую сессию). Все теги-релизы стоят на коммитах, которые
  входят в `main`.
- Теги `vX.Y.Z` — единственные метки релизов. Сборку запускает только пуш тега
  (`build-release.yml`: `on: push: tags: v*`), поэтому ни мерж в `main`, ни пуш
  обычной ветки релиз НЕ собирают и ничего не публикуют.
- **Правда о теге одна — тег в origin.** Локальный тег, который разошёлся с
  origin, — это авария: `fetch --tags` начинает падать с
  `(would clobber existing tag)`, а `sync-main.ps1` из-за этого
  останавливается. Лечение: `scripts/fix-tags.ps1` (сначала `-DryRun`).
  Ставить тег вручную в обход `scripts/make-release.ps1` нельзя.
- Рабочая ветка сессии `arena/<id>-apumir` — одна живая, пока идёт сессия.
  Она обязана быть потомком `main` (иначе `main` не обновить fast-forward).
- Всё остальное — мусор. Ветку «на память» не держим: история и так в `main`,
  релизы — в тегах и GitHub Releases.

**Что писать владельцу при входе в новый чат:** всегда **`main`** — не ветку
прошлого чата. Arena сама создаёт агенту ветку `arena/<id>-apumir`, и агент
обязан в конце сессии догнать `main` (порядок ниже), поэтому на входе любого
следующего чата `main` уже содержит всю работу.

**Правило владельца от 2026-08-30: `main` синхронизируется СРАЗУ после
каждого шага, а не в конце сессии.** Каждый запушенный коммит рабочей ветки в
том же ходе догоняется в `main` (fast-forward), и после пуша проверяется
`git ls-remote origin refs/heads/main`. Смысл: `main` не имеет права отставать
ни на один коммит, потому что следующий чат, владелец и любой релиз читают
именно её. Агент делает это сам, не дожидаясь напоминания; если fast-forward
невозможен, это останавливает работу и обсуждается, а не откладывается.

**Порядок в конце сессии (обязательно):

1. `git fetch origin main` → `git merge origin/main` (втянуть `main` в рабочую
   ветку, разрулить конфликты в пользу рабочего кода);
2. `git push origin <рабочая ветка>`;
3. обновить `main`: fast-forward-мерж рабочей ветки в `main` — через PR,
   который жмёт владелец, либо одной командой на его ПК (**powershell**):
   `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\sync-main.ps1`;
4. проверить: `git ls-remote origin refs/heads/main` = рабочему кончику.

**Запрещено:**

- держать релиз отдельной веткой (`release-*`) вместо тега;
- принимать в `main` чужие `.gitignore`/сборочные файлы не глядя: глобальный
  `*.so` в `.gitignore` ломает отслеживание
  `android-app/app/src/main/jniLibs/*/libp2p_core.so`;
- плодить ветки: одна сессия — одна ветка, и она продолжает предыдущую,
  а не начинает с `main`.

**Что случилось и почему (2026-08-29, разбор для владельца):**

1. `main` не догоняли с 2026-08-17: в нём висел `6d28249`, а 628 коммитов
   раундов 11–46 жили только в ветках `arena/*`.
2. Каждая новая Arena-сессия создавала СВОЮ ветку, а не продолжала прошлую —
   накопилось 12 веток `arena/*` (в 11 из них не было ни одного уникального
   коммита).
3. 2026-08-17 в `main` влили PR #3 от бота (`qwen.ai[bot]`, ветка
   `instructions-for-the-new-ai-agent-bc4fe`): три файла
   (`create_store_forward.py`, `rust-core/src/network/store_and_forward.rs`,
   `scripts/setup_offline_delivery.ps1`) — Rust-модуль нигде не подключён к
   сборке, не гейтован и не релизнут, плюс переписанный `.gitignore`
   (начинался с markdown-забора ```rust и игнорировал `*.so`). При выравнивании
   2026-08-29 эти файлы сохранены в истории как есть, а `.gitignore` и
   `rust-core/src/network/mod.rs` оставлены рабочие — чужой модуль в сборку
   НЕ включён.
4. Релиз v11.16.1 выпустили веткой `release-v11.16.1`, а не тегом: коммит
   `93d6a4c` (versionName v11.16.1) не входит в тег `v11.16.1` (тег стоит на
   `8ed534e`). Сегодня это не важно: версию берёт из имени тега
   `build-release.yml` (`Sync versionName with tag`).
5. Старые ветки удалены после проверки «в ней нет ничего, чего нет в `main`»
   (`git rev-list --count main..<ветка>` = 0). Проверка перед удалением
   обязательна.

## 10. Копия на флешке (правило от 2026-08-30)

`scripts/backup-to-usb.ps1 -Drive E:` кладёт на флешку полную историю git
(зеркало + бандл одним файлом), файлы, которых в git нет (`local.properties`,
`.env`, собранные APK), незакоммиченные правки патчем, `MANIFEST.txt` с sha256
и скрипт восстановления. Подробно — `docs/BACKUP_AND_NEW_PC.md`.

Запускать после каждого релиза и перед любой поездкой. Бэкап создаёт новую
папку с датой и ничего не перезаписывает, так что старая копия остаётся точкой
отката.

Три вещи, которые скрипт делает не зря (все три пойманы на реальных ошибках):

1. Отказывается работать на поверхностном клоне и **реально клонирует** бандл
   обратно, а не верит `git bundle verify`: тот печатает «records a complete
   history» даже для бандла из `--depth`-клона, и такой бандл падает при
   восстановлении.
2. Сверяет локальные ветки с `origin/*` в зеркале. Клон зеркала мапит только
   `refs/heads/*`, поэтому отставшая локальная ветка утащила бы новый ПК назад;
   скрипт подтягивает её, если `origin` впереди, и никогда не выбрасывает то,
   что впереди локально.
3. Кладёт `restore-from-usb.ps1` в корень копии: на новом ПК репозитория ещё
   нет, и взять скрипт больше неоткуда.

Флешка после бэкапа содержит ключ подписи релиза (`p2p-release.jks` лежит в
git, но на флешке он оказывается в распакованном виде). Потерять её не страшно,
дать уйти не туда — страшно.

`origin` после восстановления скрипт перенаправляет на GitHub сам: иначе первый
же `git push` записал бы обратно на флешку.
