# APU v11.74.54 — ссылки «Поделиться» без GitHub (vzhem спрятан)

## Что изменилось

Владелец (скрин «Поделиться профилем»): «можно спрятать vzhem и 1985,
но чтобы все ссылки работали».

- installLink: github.com/vzhem/APUMIR/releases/latest ->
  <воркер>/update/apk - воркер УЖЕ отдавал поток последнего APK
  (путь существовал для обновлений): GitHub из UI убран полностью.
- Короткие ссылки: сначала переключил на apumir.app (OFFICIAL_HOST из
  ReferralInviteLink, App Links в манифесте) - владелец УТОЧНИЛ:
  «у меня нет ресурса apumir.app» -> ОТКАТ на рабочий хост воркера.
  УРОК: OFFICIAL_HOST в коде не доказательство владения доменом -
  спросить владельца ДО переключения внешних ссылок.
- apu://-ссылка и t.me-запаска без изменений; сеть (REGISTRY_URL,
  UpdateChecker) не тронута.

## Технические детали

- ShortLinks.build: WEB_HOST (возврат); PATTERN принимал оба хоста и
  продолжает - старые ссылки работают.
- «1985vzhem» в адресах без нейтрального домена владельца не прячется:
  вариант - купить домен и привязать к воркеру (потом одна константа +
  манифест); переименовывать workers.dev subdomain НЕЛЬЗЯ (сломает
  старые телефоны: relay/update захардкожены). ВЛАДЕЛЕЦ В КУРСЕ.
- Отдельная тревога (владелец решил пока не разбирать): ReferralInviteLink
  (подписанные приглашения) указывает на apumir.app - если домен не наш,
  эта функция ходит на чужой сервер. ПРОВЕРИТЬ ОТДЕЛЬНО.
- Откаты: 11-й посреди раунда (281ca9b был уже запушен; правки отката
  применил повторно после reset). check90/91 success.
  v11.74.54 = 50a0960, run 36054425368 SUCCESS, latest, APK
  40 500 694 Б, sha256 dabe9e81…815.
