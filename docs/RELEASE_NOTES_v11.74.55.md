# APU v11.74.55 — «Отправить в APU»: рассылка приглашений контактам

## Что изменилось

Владелец (скрин «Пригласить в группу»): третий вариант «отправить
ссылку в APU» - открывается список контактов приложения, галочки,
«Отправить» появляется при выборе, рассылка в личные чаты, ограничение
100 человек.

- ChatListScreen: в диалог приглашения добавлена кнопка «Отправить
  в APU» -> диалог «Кому отправить»: LazyColumn личных чатов
  (getAllChats, сортировка по имени) + Checkbox, счётчик «Выбрано:
  N из 100», кнопка «Отправить» появляется только при выбранной
  галочке (владелец: «ниже при выборе появляется»), Toast «Отправлено:
  N (, не удалось: M)».
- ChatListViewModel: personalChatsOnce(); sendGroupInviteToChats -
  inviteLinkFor (короткая ссылка) + sendMessage(chatId, contactId,
  «Приглашение в группу «Название»:\n<link>») каждому; лимит
  MAX_INVITE_RECIPIENTS = 100 (и в UI, и в цикле).
- ChatRepository.getAllChats().

## Технические детали

- Приглашение едет обычным личным сообщением: сосед видит карточку
  с ссылкой, вход по ней - прежний путь (оба хоста в App Links).
- Грабли: python-heredoc съел \n в Kotlin-литерале (реальный перенос
  в строке -> compile error) - check92 failure, фикс \\n, check93
  success. УРОК: в питоне, генерирующем Kotlin, писать \\n как
  \\\\n. 12-й откат посреди раунда (спасение /tmp/CR,CLVM,CLS +
  reset + возврат). APK вырос на ~16 КБ (новый UI).
- v11.74.55 = 03e462c, run 36092727698 SUCCESS, latest, APK
  40 517 078 Б, sha256 68a7f215…354.
