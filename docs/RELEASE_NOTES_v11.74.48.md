# APU v11.74.48 — переписка не прячется за пузыри ввода

## Что изменилось

Лента тем/каналов при открытой клавиатуре сжимается над ней
(imePadding на LazyColumn ленты): последнее сообщение всегда видно
над пузырями поля - и отправленное, и входящие. Причина бага:
edge-to-edge - окно само не сжимается клавиатурой; пузырям подъём
дан в р.147/149, лента - только сейчас (владелец: «переписка так и
прячется за пузырём ввода и пузырём отправки»).

## Технические детали

- GroupChatScreen лента: modifier `Modifier.weight(1f).fillMaxWidth()
  .imePadding()`; запас bottom 200dp (р.147) теперь ровно отводит
  место под оверлей (пилюля + поле + «Отправить»).
- Автопрокрут р.150 (после отправки) работает поверх нового вьюпорта.
- check82 зелёный. v11.74.48 = 0fc9b95, run 35984834411 SUCCESS,
  latest, APK 40 500 690 Б, sha256 004ccdb2…1ca.
