# APU v11.74.77 — аудит-2: два тихих снижения нагрузки

## Что изменилось (раунд 180)

Владелец: «запускай аудит с этими правками».

1. **markOnline по гейту** (CoreServerService, peer_discovered):
   регистр «кто онлайн» для файлов (fileTransferRouter.markOnline)
   обновляется ТОЛЬКО на «тяжёлом» пульсе (раз в 30 с, условие
   !lightTouch), а не на каждый пульс. Раньше при живой сети - десятки
   лишних запусков корутин в минуту без пользы. Запросы сидов групп
   и APK-раздачи (onPeerOnline) не тронуты - они и были за гейтом.
2. **Кэш сжатого аватара** (GroupsModule.myAvatarB64 + MyAvatarMemo):
   AvatarCompress.compressUri зовётся только когда сменился uri
   аватара; раньше каждый ответ «представься» (whois) и каждая
   рассылка сжимали картинку заново (ЦП + батарея).

Поведение и E2E не изменены. Грабли: первый вариант - объект до
импортов (check133 fail «imports are only allowed in the beginning»),
перенесён в конец файла; check134 success.

## Данные

v11.74.77 = 4ca51eb, run 36267512069, Latest, APK 40 582 294 Б,
sha256 428345ed…e92.
