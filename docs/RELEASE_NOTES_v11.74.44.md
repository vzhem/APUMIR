# APU v11.74.44 — поле ввода над всей шириной экрана; «Отправить» не прячется

## Что изменилось

- Окно реагирует на клавиатуру правильно (adjustResize + imePadding):
  пузырь «Отправить» виден всегда, включая набор текста (раньше система
  задвигала низ экрана под клавиатуру).
- Поле ввода - оверлей НА ВЕСЬ экран, включая левую область пузырей тем
  (владелец: «расширить на левую область... поверх этой области»).
- Лента темы получила нижний запас (contentPadding bottom 200dp) -
  последнее сообщение не прячется за оверлеем.
- Пузырёк «↓ Ещё N сообщений» переехал в оверлей, прямо над полем.

## Технические детали

- AndroidManifest: MainActivity + windowSoftInputMode="adjustResize".
- GroupChatScreen: блок «пилюля + поле ввода» вырезан из правой колонки
  и вставлен в root Box (Modifier.align(BottomCenter).fillMaxWidth()
  .imePadding()) ПОСЛЕ Scaffold - накрывает и рельсу тем; feed
  contentPadding bottom 200dp.
- Стейты ленты (feedListState/feedScope/LaunchedEffect/feedRemaining)
  подняты из правой колонки на уровень функции (нужны оверлею).
- Грабли check76: оверлей вставился ЗА root Box (align не в BoxScope) +
  стейты в скоупе колонки; фикс: подъём стейтов перед Box, оверлей
  перед закрывающей Box. check77 зелёный.
- v11.74.44 = 3f2bb3b, run 35965422878 SUCCESS, latest,
  APK 40 500 694 Б, sha256 55439571…6ab.
