# APU v11.74.42 — просторное поле ввода: кнопки уходят наверх при наборе

## Что изменилось

- Поле ввода в темах/комментариях: до 6 строк (было 4).
- При фокусе на поле скрепка и GIF переезжают в строку НАД полем
  (внутри того же пузыря-подложки), текст занимает всю ширину.
- Фокус снят (отправили/отошли) - кнопки возвращаются в ряд с полем.
- «Отправить» всегда справа от текста, выровнена по низу поля.

## Технические детали

- GroupChatScreen: блок поля ввода Row → Column (пузырь тот же);
  `var inputFocused by remember`; на OutlinedTextField -
  `Modifier.onFocusChanged { inputFocused = it.isFocused }`
  (import androidx.compose.ui.focus.onFocusChanged);
  if (inputFocused) { Row { скрепка; GIF } }; нижний Row
  (Alignment.Bottom): TextField(weight 1f, minLines 1, maxLines 6) +
  Spacer + «Отправить».
- Грабли: python-замена по якорям не совпала (реконструкция блока) -
  замена по номерам строк с assert-ами содержимого; хвост старого Row
  (`}` строки 731) остался после вставки (лишняя скобка, struct_check
  поймал) - удалён дублирующий `}`. Итог: +69/-60, один файл.
- check74 зелёный. v11.74.42 = 99da286, run 35957925589 SUCCESS,
  latest, APK 40 500 046 Б, sha256 30fc8e96…787.
